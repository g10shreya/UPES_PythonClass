length = float(input('Enter the length of rectangle (in cm):')) #taking length as input from user in cm
width = float(input('Enter the width of rectangle (in cm):')) #taking width as input from user in cm

# print('The Area of the given rectangle will be:', length * width, 'cm sq') 
print(f"{length * width:.2f}") #area calculated with 2 decimal places